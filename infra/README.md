# Infra

Deployment and CI/CD assets will live here.

Initial recommendation:
- add CI for backend tests and frontend build first
- add containerization after the local vertical slice stabilizes
- add cloud deployment only after retrieval and eval workflows are defined
