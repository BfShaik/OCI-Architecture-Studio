name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dependencies
        working-directory: app/backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ingestion smoke test
        run: |
          python knowledge/ingestion/ingest.py --no-fetch
          python knowledge/refresh/ingest_releases.py --no-fetch

      - name: Retrieval health check
        run: |
          python infra/scripts/check_retrieval_health.py --provider local_json

      - name: Retrieval regression check
        run: |
          python infra/scripts/retrieval_regression_check.py \
            --cases evals/golden-prompts.jsonl \
            --cases evals/edge-cases.jsonl \
            --output-dir evals/reports/retrieval

      - name: Deployment config template validation
        run: |
          python infra/scripts/validate_deployment_config.py \
            --tfvars infra/terraform/envs/staging/terraform.tfvars.example \
            --allow-placeholders

      - name: Backend tests
        working-directory: app/backend
        env:
          PYTHONPATH: src
        run: |
          pytest -q

      - name: Golden prompt evals
        run: |
          python evals/run_golden.py --output-dir evals/reports/golden

      - name: Edge-case evals
        run: |
          python evals/run_golden.py --cases evals/edge-cases.jsonl --output-dir evals/reports/edge-cases

      - name: Advisory-quality evals
        run: |
          python evals/run_golden.py --cases evals/advisory-quality.jsonl --output-dir evals/reports/advisory-quality

      - name: Controlled orchestration evals
        run: |
          python evals/run_golden.py --cases evals/orchestration-quality.jsonl --output-dir evals/reports/orchestration-quality

      - name: GenAI synthesis parity readiness
        run: |
          python infra/scripts/genai_synthesis_parity_check.py \
            --cases evals/golden-prompts.jsonl \
            --output-dir evals/reports/genai-parity \
            --allow-skip

      - name: Upload eval reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: golden-eval-reports
          path: evals/reports/

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: app/frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: app/frontend
        run: npm ci

      - name: Frontend build
        working-directory: app/frontend
        run: npm run build
